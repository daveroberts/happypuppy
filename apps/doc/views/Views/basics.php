<h1>Using Views in your Happy Puppy application</h1>
<p>In the <?php echo link_to("last example", "/Applications/Create")?> you created a new Happy Puppy application, a controller in that application, and an action in that controller.</p>
<p>The controller's action just printed some text, then set a flag to tell Happy Puppy to send that text to the web browser directly</p>
<p>Normally, we want to show an HTML page, not just print text.  To do this, we need to use </p>